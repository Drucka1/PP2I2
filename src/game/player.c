#include "../../include/game/player.h"

// void initPlayer(Map *map, SDL_Renderer *renderer) 
// {
//   Entity *player = initCell(GRID_HEIGHT/2, GRID_WIDTH/2, "player", renderer);
//   player->entity.steppable = 0;
//   
//   freeCell(player->row, player->column, map);
//   map->data[player->row][player->column] = player;
// }

 
