# PPII 2


git branch blabla
git checkout blabla

git pull origin master
git push --set-upstream origin blabla

LES TOUCHES :
    a : active/desactive en place un assombrissement
    e : interaction avec object (pour l'instant seulement les clés)
    echap : quit de jeu
    i : active/desactive l'état de glace (mouvement impossible)
    r : place le compteur de mouvement restant à 10

Il manque l'affichage.

Rapidement nouvelle variable cleared qui determine si une salle est finie elle passe à true lorsque l'on passe la porte vers le niveau suivant.



IMPORTANT il faut rajouter le nombre de pas limite dans le fichier txt qui determine la map sur la premiere lignes par exemple 10 10 32<----- le nombre de pas limite  